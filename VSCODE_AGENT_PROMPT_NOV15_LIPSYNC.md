# Zero-Shot Prompt for VS Code Agent Mode
## Goal: Kelly Avatar Lipsync with ElevenLabs Voice - Nov 15 Balance Lesson

Copy and paste this entire prompt into VS Code Agent Mode:

---

**OBJECTIVE:** Set up complete Unity WebGL integration with Kelly avatar lipsynced to ElevenLabs Kelly voice for the November 15 Balance lesson. Fix C# language server errors, deploy Unity build to public folder, generate missing audio files, create viseme JSON for lipsync, and configure the lesson player to display Kelly speaking with perfect lip synchronization.

**ARCHITECTURE (CRITICAL):**
- **Claude writes lessons** → Lesson JSON files (text only, no audio)
- **Cursor (UI-TARS-desktop) generates audio** → Calls ElevenLabs API at BUILD-TIME, saves MP3 files
- **Unity (VS Code "My project") plays audio** → Loads pre-generated MP3 + viseme JSON at RUNTIME
- **Unity NEVER calls ElevenLabs API** - Unity only loads pre-generated files via HTTP
- **Audio generation happens ONCE** when lesson is created, NOT every time Unity runs

**CONTEXT:**
- Unity project location: `digital-kelly/engines/kelly_unity_player`
- Lesson file: `lesson-player/balance-schema-compliant.json` (Nov 15, 2024-11-15)
- ElevenLabs Voice ID: `wAdymQH5YucAkXwmrdL0` (Kelly voice)
- Target: See Kelly avatar lipsynced with audio in browser
- Unity build needs to be in `public/unity/kelly-v1/` for web server access
- Audio files need to be in `lessons/audio/balance/` with format: `{age}-{lang}-{phase}.mp3`
- Viseme JSON files needed for Audio2Face/Unity lipsync in format: `{age}-{lang}-{phase}.a2f.json`
- **All audio must be pre-generated by Cursor BEFORE Unity tries to load it**

**TASKS:**

1. **Fix C# Language Server Error**
   - The OUTPUT panel shows: `[LanguageServerProjectLoader] Error` related to MSBuild
   - Check `.vscode/settings.json` for C# extension configuration
   - Verify `digital-kelly/engines/kelly_unity_player/*.slnx` and `.csproj` files are valid
   - Add/update OmniSharp or C# Dev Kit settings to exclude Unity temp files
   - Ensure Unity project structure is recognized (check for `Assets/`, `ProjectSettings/` folders)
   - If needed, add `.vscode/settings.json` entry: `"omnisharp.useModernNet": false` or configure Unity-specific paths

2. **Generate Missing Audio Files for Balance Lesson (BUILD-TIME - Cursor's Responsibility)**
   - **CRITICAL:** This happens in Cursor (UI-TARS-desktop), NOT in Unity
   - **CRITICAL:** This is BUILD-TIME generation, NOT runtime
   - Read `lesson-player/balance-schema-compliant.json` to extract all text content
   - For each age variant (2-5, 6-12, 13-17, 18-35, 36-60, 61-102):
     - Extract `welcome`, `mainContent`, and `wisdomMoment` text from each language (en, es, fr)
     - Use `scripts/generate_all_lesson_audio.py` to generate audio via ElevenLabs API
   - Generate 54 audio files total (6 ages × 3 languages × 3 phases) using ElevenLabs API
   - Save to `lessons/audio/balance/{age}-{lang}-{phase}.mp3`
   - Use voice ID `wAdymQH5YucAkXwmrdL0` with age-specific voice settings from lesson JSON
   - Create `lessons/audio/balance/metadata.json` with file listing
   - **Verify all 54 files exist before proceeding** - Unity cannot play lessons without pre-generated audio

3. **Generate Viseme JSON Files for Lipsync**
   - For each generated audio file, create corresponding Audio2Face viseme JSON
   - Use Audio2Face API or `scripts/generate_kelly_clip_audio.py` pattern to generate visemes
   - Format: `lessons/audio/balance/{age}-{lang}-{phase}.a2f.json`
   - Each JSON should contain viseme timing data synchronized with audio file
   - Reference `ELEVENLABS_ICLONE_INTEGRATION_EXPLAINED.md` for viseme mapping
   - Ensure JSON format matches Unity's `BlendshapeDriver` expectations (53 blendshapes)

4. **Build and Deploy Unity WebGL to Public Folder**
   - Run `scripts/deploy_unity_webgl.ps1` to build Unity project
   - If Unity path not found, detect Unity installation automatically
   - Build output should go to `digital-kelly/engines/kelly_unity_player/Builds/WebGL/kelly-v1/`
   - Copy entire build to `public/unity/kelly-v1/` (this is what you mentioned needing to move to public)
   - Ensure `public/unity/kelly-v1/index.html` includes `kbridge.js` script reference
   - Verify `public/unity/kelly-v1/kbridge.js` exists (messaging bridge for iframe communication)
   - Check that Unity build includes `KellyBridge.cs` and `BlendshapeDriver.cs` scripts

5. **Configure Lesson Player for Balance Lesson (RUNTIME - Unity's Responsibility)**
   - **CRITICAL:** Unity only loads pre-generated files. It NEVER calls ElevenLabs API.
   - Update `lesson-player/index.html` or create test page to load Balance lesson
   - Configure Unity iframe to load from `public/unity/kelly-v1/index.html`
   - Set up PostMessage communication to send lesson data to Unity:
     ```javascript
     {
       destination: 'kelly-webgl',
       type: 'kelly-load',
       payload: {
         lessonId: 'balance-18-35-en',
         jsonUrl: '/lessons/audio/balance/18-35-en-mainContent.a2f.json',  // Pre-generated by Cursor
         audioUrl: '/lessons/audio/balance/18-35-en-mainContent.mp3',      // Pre-generated by Cursor
         expressionsUrl: '/lessons/audio/balance/18-35-en-expressions.json' // Optional, pre-generated
       }
     }
     ```
   - **Verify files exist** before sending message - Unity will fail if files don't exist
   - Ensure audio and viseme JSON URLs are accessible from web server
   - Test that Unity receives message and loads files via UnityWebRequest (NOT API calls)
   - Unity's `KellyBridge.cs` should load files via HTTP, not generate them

6. **Unity Server Configuration**
   - Ensure web server can serve files from `public/unity/kelly-v1/` (Unity build files)
   - Ensure web server can serve files from `lessons/audio/balance/` (audio + viseme JSON)
   - Check CORS headers if needed for Unity WebGL
   - Verify Unity build uses correct base path for asset loading
   - Test that Unity can load audio and JSON files via HTTP (not file://)

7. **Test End-to-End Lipsync**
   - Start web server (should auto-start via VS Code task)
   - Open `lesson-player/index.html` or test page
   - Load Balance lesson for age 18-35, English (as example)
   - Verify:
     - Unity iframe loads successfully
     - Kelly avatar appears
     - Audio plays
     - Viseme JSON loads
     - Blendshapes animate in sync with audio
     - Kelly's mouth moves correctly for speech
   - Check browser console for errors
   - Verify PostMessage communication between page and Unity

8. **Documentation Updates**
   - Update `UNITY_DEPLOYMENT_STATUS.md` with completion status
   - Document audio file locations and naming convention
   - Document viseme JSON format and mapping
   - Add troubleshooting notes for common issues

**SUCCESS CRITERIA:**
- ✅ C# language server error resolved (no errors in OUTPUT panel)
- ✅ All 54 audio files generated in `lessons/audio/balance/`
- ✅ All 54 viseme JSON files generated in `lessons/audio/balance/`
- ✅ Unity WebGL build deployed to `public/unity/kelly-v1/`
- ✅ Lesson player loads Balance lesson
- ✅ Kelly avatar displays and speaks
- ✅ Lipsync works - mouth moves in sync with ElevenLabs Kelly voice
- ✅ Audio plays correctly
- ✅ No console errors

**CONSTRAINTS:**
- Follow `CLAUDE.md` rules: no browser TTS, use ElevenLabs only
- **CRITICAL:** Audio generation happens in Cursor (build-time), NOT in Unity (runtime)
- **CRITICAL:** Unity NEVER calls ElevenLabs API - Unity only loads pre-generated files
- Maintain 60 FPS target for Unity avatar
- Use existing Unity scripts (`KellyBridge.cs`, `BlendshapeDriver.cs`)
- Don't modify Unity project structure unnecessarily
- Keep audio generation scripted/reproducible
- Ensure all files are in correct locations for web server access
- **Verify all 54 audio files + 54 viseme JSON files exist before Unity tries to load them**

**REFERENCES:**
- `UNITY_WEBGL_BUILD_GUIDE.md` - Unity build process
- `WEBGL_EMBED_GUIDE.md` - Unity iframe integration
- `ELEVENLABS_ICLONE_INTEGRATION_EXPLAINED.md` - Viseme mapping
- `CONTENT_STATUS_NOV15.md` - Balance lesson status
- `scripts/generate_all_lesson_audio.py` - Audio generation script
- `scripts/deploy_unity_webgl.ps1` - Unity deployment script

**START HERE:** Begin by fixing the C# language server error, then proceed through tasks sequentially. Verify each step before moving to the next. Test frequently to catch issues early.

---

**END OF PROMPT**

