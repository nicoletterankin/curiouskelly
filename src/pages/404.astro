---
import SiteLayout from '@layouts/SiteLayout.astro';
import { getDictionary, defaultLocale } from '@lib/i18n';

const locale = defaultLocale;
const dictionary = getDictionary(locale);
---

<SiteLayout dictionary={dictionary} locale={locale} currentPath="/404" routeKey="home" title="Page not found — Curious Kelly">
  <section class="py-5">
    <div class="container text-center">
      <h1 class="display-4 fw-semibold">Page not found</h1>
      <p class="lead text-muted">The link you followed might be broken or the page may have been removed.</p>
      <a class="btn btn-gradient" href="/">Return home</a>
    </div>
  </section>
</SiteLayout>




