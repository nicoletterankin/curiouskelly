# Remove LFS tracking for files not needed in Cloudflare Pages build
# PNG and WAV files are causing build failures - lesson player uses MP3, not WAV
*.png -filter -diff -merge
*.wav -filter -diff -merge

# LFS tracking for large files only (if needed for other builds)
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.tif filter=lfs diff=lfs merge=lfs -text
*.iProject filter=lfs diff=lfs merge=lfs -text
*.ccProject filter=lfs diff=lfs merge=lfs -text
