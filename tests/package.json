{
  "name": "@acme/tests",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "test:unit": "pnpm exec vitest run --config vitest.config.ts --dir unit",
    "test:integration": "pnpm exec vitest run --config vitest.config.ts --dir integration",
    "test:e2e": "pnpm exec playwright test",
    "test:load": "pnpm exec k6 run load/join.js",
    "test:all": "pnpm run test:unit && pnpm run test:integration"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/supertest": "^2.0.16",
    "k6": "^0.49.0",
    "supertest": "^7.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}

