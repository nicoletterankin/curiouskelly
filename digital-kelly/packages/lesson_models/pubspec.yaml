name: lesson_models
description: Shared Dart models and schemas for lesson data
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0


















