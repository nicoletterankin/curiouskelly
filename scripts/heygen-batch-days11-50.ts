#!/usr/bin/env npx tsx
/**
 * Alias script (operator clarity):
 * Runs the HeyGen production batch for Days 11-50.
 *
 * This repo’s original batch implementation lives in `scripts/heygen-batch-days1-10.ts`
 * (despite the filename, it targets Days 11-50). This file preserves the expected
 * command name from the deployment checklist.
 *
 * Run:
 *   npx tsx scripts/heygen-batch-days11-50.ts
 */
import './heygen-batch-days1-10';

