---
import SiteLayout from '@layouts/SiteLayout.astro';
import { getTranslations, getPathPrefix, type Locale } from '@lib/i18n';

const locale: Locale = 'en-US';
const t = getTranslations(locale);
const prefix = getPathPrefix(locale);
---

<SiteLayout title={`Thank You - ${t.site.name}`} description="Thank you for your submission" locale={locale} path="/thank-you/">
  <section class="thank-you-section">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-8 text-center">
          <div class="thank-you-icon">✓</div>
          <h1>{t.thankYou.title}</h1>
          <p class="lead">{t.thankYou.message}</p>
          <a href={prefix || '/'} class="btn btn-primary btn-lg">
            {t.thankYou.backHome}
          </a>
        </div>
      </div>
    </div>
  </section>
</SiteLayout>

<style>
  .thank-you-section {
    padding: 6rem 0;
    min-height: 60vh;
    display: flex;
    align-items: center;
  }

  .thank-you-icon {
    width: 100px;
    height: 100px;
    margin: 0 auto 2rem;
    background: #28a745;
    color: #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    font-weight: 700;
  }

  .thank-you-section h1 {
    margin-bottom: 1.5rem;
    font-size: 3rem;
  }

  .lead {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    color: #6c757d;
  }
</style>










