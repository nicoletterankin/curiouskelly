---
import { generateSitemap, generateSitemapRoutes } from '../lib/sitemap.js';

const site = Astro.site?.href || 'https://thedailylesson.com';
const routes = generateSitemapRoutes(Astro);
const sitemap = generateSitemap(site, routes);
---

{sitemap}

